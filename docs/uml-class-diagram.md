# UML Class Diagram

The editable source of the class diagram is in:

- `docs/class-diagram.puml`

Rendered export:

- `docs/class-diagram.png`

To regenerate locally (when PlantUML is installed):

```bash
plantuml -tpng docs/class-diagram.puml
```
